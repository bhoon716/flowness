# Master Plan Compliance Matrix

Current release-readiness matrix for Flowness v0.1.

| Requirement ID | Area | Requirement | Implementation Location | Evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | Project Structure | TypeScript-first npm workspace, package boundaries, and Flowness-managed workspace scaffolding are present. | `package.json`, `tsconfig.base.json`, `packages/*`, `packages/core/src/scaffold.ts` | `npm run build`; `npm test`; `flowness init <temp-sandbox>` | PASS | Workspace layout matches the current monorepo and init scaffold. |
| R-02 | Issue | Requests become issues through the CLI and workspace scaffolding. | `packages/issue-system/src/index.ts`, `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `runCli captures a request as an issue`; `runCli auto-captures freeform requests as issues` | PASS | `issue:create` and `request:create` both create issue workspaces. |
| R-03 | Issue | Issue naming, issue type normalization, and issue state handling are enforced. | `packages/issue-system/src/index.ts`, `packages/cli/src/index.ts` | `issueTypeValues`; `createIssueId`; issue workspace tests | PASS | Names are slugified and issue types are constrained to the supported set. |
| R-04 | Log | Every issue gets an append-only log with an initial entry. | `packages/log-system/src/index.ts`, `packages/issue-system/src/index.ts`, `.agent/audit/audit-log.md` | `runCli creates an issue workspace and initial log`; audit log history is append-only | PASS | Initial issue log creation is wired into issue creation. |
| R-05 | Log | Logs are append-only and do not overwrite existing history. | `packages/log-system/src/index.ts` | `appendLogEntryToIssue`; current audit log entries preserved across phases | PASS | Existing log files are appended to, not rewritten. |
| R-06 | Decision | Decision documents are created, validated, and linked to issues. | `packages/decision-system/src/index.ts`, `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `decision documents validate and persist to issue directories`; `decision documents reject missing mandatory sections` | PASS | Mandatory sections and issue linkage are enforced. |
| R-07 | Evidence | Evidence records are validated and workflow completion requires evidence. | `packages/evidence-system/src/index.ts`, `packages/workflow-engine/src/runtime.ts`, `packages/cli/src/index.ts` | `evidence helpers validate and summarize records`; `runWorkflowStep handles human gates, transitions, and failure recovery` | PASS | Evidence is required for completion and file-backed evidence is checked. |
| R-08 | Workflow Engine | Workflow definitions load from built-ins, workspace TypeScript, Python, and Shell sources. | `packages/workflow-engine/src/loader.ts`, `packages/workflow-engine/src/builtins.ts`, `packages/cli/src/index.ts` | `loadWorkflowDefinitionFromFile supports python workflow blueprints`; `...shell workflow blueprints`; `built-in workflows validate` | PASS | Loader support matches the release scope. |
| R-09 | Workflow Engine | Workflow execution, step transitions, and state persistence are implemented. | `packages/workflow-engine/src/runtime.ts`, `packages/issue-system/src/index.ts` | `runWorkflowStep handles human gates, transitions, and failure recovery`; `runCli advances workflows, records decisions, and runs reviews` | PASS | Step state moves forward through persisted workflow state. |
| R-10 | Workflow Engine | Skipped workflow states are rejected. | `packages/workflow-engine/src/runtime.ts`, `packages/cli/src/index.test.ts` | `runWorkflowStep rejects skipped workflow states` | PASS | State corruption does not bypass step order. |
| R-11 | Human Gate | Human gate instructions are configurable and can be updated from natural language. | `packages/core/src/config.ts`, `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts`, `packages/core/src/config.test.ts` | `human gate instructions can be applied from natural language`; `runCli applies human gate instructions from natural language` | PASS | `config:gate` is reachable and wired to config parsing. |
| R-12 | Recovery | Recovery loops record root cause, retry the step, and revalidate the workflow. | `packages/workflow-engine/src/runtime.ts`, `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `runCli recover command retries a fixed workflow step` | PASS | Recovery is not a dead-end; it re-enters the workflow after root cause capture. |
| R-13 | Review System | Reviews run through independent reviewers and are aggregated. | `packages/review-system/src/index.ts`, `packages/cli/src/index.ts`, `packages/review-system/src/index.test.ts` | `review coordinator aggregates independent reviewers`; `runCli advances workflows, records decisions, and runs reviews` | PASS | Review execution is subprocess-based per role. |
| R-14 | Built-In Workflows | Built-in workflows exist for the product’s core issue types. | `packages/workflow-engine/src/builtins.ts`, `packages/cli/src/index.ts` | `built-in workflows validate`; built-in IDs: `planning`, `mvp`, `greenfield`, `research`, `feature`, `bugfix`, `refactor`, `harness` | PASS | The built-in set matches the supported release scope. |
| R-15 | Prompt Architecture | Initialization creates the prompt scaffold set for the core agent roles. | `packages/core/src/scaffold.ts`, `packages/core/src/scaffold.test.ts` | `initializeProject creates the Flowness project skeleton`; `flowness init <temp-sandbox>` output | PASS | Core, planning, review, research, and architecture prompt files are generated. |
| R-16 | Scripts | Initialization creates the workspace script scaffold set, and the release audit script exists. | `packages/core/src/scaffold.ts`, `scripts/audit-v0.1.mjs` | `initializeProject creates the Flowness project skeleton`; `flowness init <temp-sandbox>` output | PASS | `find-fqcn.py`, `search-reference.py`, and `check-md-size.py` are scaffolded. |
| R-17 | Skills | Skill creation, listing, and execution are implemented and reachable from the CLI. | `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `runCli creates skill and rule scaffolds and validates the workspace`; `skill:list`; `skill:run` | PASS | Skill registry and execution commands are available. |
| R-18 | Rules | Rule creation, listing, and application are implemented and reachable from the CLI. | `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `runCli creates skill and rule scaffolds and validates the workspace`; `rule:list`; `rule:apply` | PASS | Rule registry and execution commands are available. |
| R-19 | NPM Package | The root package exports the workspace scripts and the CLI bin is wired. | `package.json`, `packages/cli/package.json` | `npm run build`; `npm test`; `npm run audit`; `flowness --help` | PASS | `npm run audit` now exists as a reproducible release check. |
| R-20 | Configuration | Configuration parsing, YAML round-trip, and gate updates are implemented. | `packages/config-system/src/index.ts`, `packages/core/src/config.ts`, `packages/core/src/config.test.ts` | `project config round-trips through yaml`; `human gate instructions can be applied from natural language` | PASS | Configuration is a first-class workspace artifact. |
| R-21 | CLI Reachability | The public CLI surface is documented and executable from the installed `flowness` command. | `packages/cli/src/bin.ts`, `packages/cli/src/index.ts`, `packages/cli/src/index.test.ts` | `flowness --help`; `flowness init <temp-sandbox>`; `flowness validate` | PASS | The command is present on PATH in this workspace and reachable in temp sandboxes. |
| R-22 | Validation | Workspace validation runs successfully after initialization. | `packages/cli/src/index.ts`, `scripts/audit-v0.1.mjs` | `flowness validate` in temp sandbox; `npm run audit` | PASS | Bare repo validation fails by design until `init` runs, but initialized workspaces validate cleanly. |
| R-23 | Future Roadmap Separation | v0.1 release artifacts separate current scope from v3 roadmap work. | `master-plan.md`, `.agent/audit/audit-log.md`, `docs/releases/v0.1-readiness.md` | release readiness doc; audit log scope note; no v3 surface in CLI/help/tests | PASS | v3 items remain explicitly out of scope for this release candidate. |

### Summary

- PASS: 23
- PARTIAL: 0
- FAIL: 0
